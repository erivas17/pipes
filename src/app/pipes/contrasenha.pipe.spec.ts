import { ContrasenhaPipe } from './contrasenha.pipe';

describe('ContrasenhaPipe', () => {
  it('create an instance', () => {
    const pipe = new ContrasenhaPipe();
    expect(pipe).toBeTruthy();
  });
});
